version: "3"
services:
  client:
    container_name: fantasticmemory:client
    restart: always
    build: .
    ports: 3000:3000
    links:
      - server
  server:
    container_name: fantasticmemory:server
    restart: always
    ports: 5000:5000
    links:
      - client
